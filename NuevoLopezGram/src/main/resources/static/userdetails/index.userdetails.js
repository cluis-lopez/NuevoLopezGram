(function () {
    'use strict';

    angular
        .module('app')
        .controller('UserDetails.IndexController', Controller);

    function Controller($) {
        var vm = this;
		
        initController();

        function initController() {
        }

    }
})();